# Pre-commit hooks will be defined here.
# This file is a placeholder and will be populated with specific hooks
# such as ruff, mypy, etc., in later phases as the toolchain is built out.

# Example structure (to be uncommented and configured later):
# repos:
# -   repo: https://github.com/pre-commit/pre-commit-hooks
#     rev: v4.5.0
#     hooks:
#     -   id: trailing-whitespace
#     -   id: end-of-file-fixer
#     -   id: check-yaml
#     -   id: check-added-large-files
# -   repo: https://github.com/astral-sh/ruff-pre-commit
#     rev: v0.1.7 # Replace with actual ruff version
#     hooks:
#     -   id: ruff
#         args: [--fix, --exit-non-zero-on-fix]
#     -   id: ruff-format
#         args: []
# -   repo: https://github.com/pre-commit/mirrors-mypy
#     rev: '' # Replace with actual mypy version
#     hooks:
#     -   id: mypy
#         additional_dependencies: [] # Add mypy dependencies here
#         args: [--strict]

ci:
    autofix_commit_msg: |
        chore: auto fixes from pre-commit.com hooks

        for more information, see https://pre-commit.ci
    autoupdate_commit_msg: |
        chore: pre-commit autoupdate
    autoupdate_schedule: weekly
